services:
  db:
    image: postgres:16
    container_name: mitene-postgres
    environment:
      POSTGRES_USER: mitene
      POSTGRES_PASSWORD: mitene_password
      POSTGRES_DB: mitene
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: